<h1> Conversations </h1>
<% @conversations.each do |conversation| %>
	<div>
		Subject: <%= link_to conversation.subject, conversation_path(conversation) %>
	</div>
<% end %>

<%= link_to "New Conversation", new_conversation_path %>